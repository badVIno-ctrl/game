# PROGRESS LOG — FPS ARENA доведение до релиза

Журнал автономной работы кодового агента. Каждая запись: что сделано, как проверено.

## Базовая линия (старт)
- Сервер запускается (`python3 main.py`, порт 8000), `/` → 200.
- Тесты на старте: `test_e2e.py` 31/31 PASS; `test_edge.py` 16/17 (флак E3 — таймаут на broadcast `team_player_left`, чиним в этапе сети).
- `game.html` ~11200 строк (Three.js фронт), `main.py` ~910 строк (FastAPI/WS).

## Состояние сервера (факт на момент ревизии)
Уже реализовано в `main.py` (этап A серверной части по большей части готов):
- `TEAM_MAX = 10` — команды до 10×10.
- Авто-баланс команд при входе и late-join в активный матч.
- Реконнект по нику (RECONNECT_TTL) с сохранением команды/спавна/счёта.
- Лимиты матча: MATCH_KILL_LIMIT, MATCH_TIME_LIMIT; корректное окончание (`team_match_end`).
- Защита от двойного начисления киллов (`dying`-guard) и friendly-fire OFF.

Основной оставшийся объём плана (этапы B–F) — во фронтенде `game.html`: оружие/анимации/звук/отдача, коллизии и карта, контент (арсенал/снаряжение), UI/UX, производительность.

## Журнал

### 2026-06-18 — Репо-гигиена (ветка `cleanup/repo-hygiene`)
- Удалён случайный артефакт `--full-page` (334 КБ, мусор без расширения).
- Удалён закоммиченный кэш байткода: `__pycache__/` и `tests/__pycache__/`.
- Добавлен `.gitignore`, чтобы кэш/виртуальные окружения/мусор больше не попадали в репозиторий.
- Новые файлы не плодились; исходники (`main.py`, `game.html`, `index.html`, тесты) не тронуты.
- Проверка: удалялись только неиспользуемые артефакты; на логику сервера/игры не влияет.
